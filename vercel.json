{
    "version": 2,
    "builds": [
        {
            "src": "api/index.js",
            "use": "@vercel/node"
        }
    ],
    "buildCommand": "npm install && npm run build",
    "rewrites": [
        { "source": "/(.*)", "destination": "/api/index.js" }
    ]
}